services:
  - type: web
    name: whatsapp-service
    runtime: docker
    dockerfilePath: ./Dockerfile
    envVars:
      - key: API_TOKEN
        sync: false
      - key: PORT
        value: 3001
      - key: NODE_ENV
        value: production
    healthCheckPath: /health
    autoDeploy: true
